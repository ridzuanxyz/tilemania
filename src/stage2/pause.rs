/// Pause menu for Stage 2 (placeholder - reuse Stage 1 pattern)

use bevy::prelude::*;
use crate::plugins::state::GameState;

/// Placeholder: Handle pause input
pub fn handle_pause_input() {
    // TODO: ESC key to pause/resume (same as Stage 1)
}

/// Placeholder: Spawn pause menu
pub fn spawn_pause_menu() {
    // TODO: Spawn pause menu overlay (reuse Stage 1 design)
}

/// Placeholder: Handle pause menu buttons
pub fn handle_pause_menu_buttons() {
    // TODO: Resume/Restart/Quit buttons (same as Stage 1)
}
